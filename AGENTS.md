# AGENTS.md

Local instructions for AI assistants (referenced by avante.nvim as `instructions_file`).

## Project conventions

- Describe the repo layout and build/test commands here as they emerge.
- Prefer small, atomic commits with problem + fix in the message.

## Secrets

- Never commit API keys or tokens. `TAVILY_API_KEY` is read from the environment.
